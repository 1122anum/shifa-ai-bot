# ============================================================
# WhatsApp Integration — requirements.txt
# Meta WhatsApp Business API version
# ============================================================

# Web framework for the webhook server
flask==3.0.3

# HTTP client — calling FastAPI backend + Meta API
requests==2.32.3

# Environment variable loading
python-dotenv==1.0.1

# Werkzeug — Flask dependency
Werkzeug==3.0.3

# Gunicorn — production WSGI server
gunicorn==22.0.0

# ngrok tunnel
pyngrok==7.2.0

# Testing
pytest==8.3.2
pytest-mock==3.14.0
responses==0.25.3
